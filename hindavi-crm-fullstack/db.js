import { DatabaseSync } from 'node:sqlite';
import { randomUUID } from 'node:crypto';

export const collections = ['leads', 'customers', 'bookings', 'visas', 'payments', 'suppliers', 'packages'];
const leadStates = ['New', 'Follow-up', 'Quotation', 'Confirmed', 'Lost'];
const bookingStates = ['Pending', 'Confirmed', 'Cancelled'];
const visaStates = ['Documents pending', 'Appointment booked', 'Submitted', 'Approved', 'Rejected'];

const seed = {
  leads: [
    { id: 'LEAD-PRIYA', name: 'Priya Patil', phone: '9876543210', destination: 'Kashmir', date: '2026-10-15', source: 'Website', status: 'New', owner: 'Snehal', followup: '2026-09-01' },
    { id: 'LEAD-RAHUL', name: 'Rahul Shah', phone: '9821011442', destination: 'Dubai', date: '2026-11-08', source: 'WhatsApp', status: 'Follow-up', owner: 'Amit', followup: '2026-09-02' },
    { id: 'LEAD-ANITA', name: 'Anita Joshi', phone: '9967233021', destination: 'Kerala', date: '2026-09-20', source: 'Instagram', status: 'Quotation', owner: 'Neha', followup: '2026-08-30' }
  ],
  customers: [
    { id: 'CUS-PRIYA', name: 'Priya Patil', phone: '9876543210', documents: 'Passport, Aadhaar', lastTrip: 'Goa · 2025' },
    { id: 'CUS-RAHUL', name: 'Rahul Shah', phone: '9821011442', documents: 'Passport', lastTrip: 'Singapore · 2024' }
  ],
  bookings: [
    { id: 'HT-26081', customer: 'Sameer More', phone: '9769088214', trip: 'Europe', type: 'International', services: 'Flight, Hotel, Visa', departure: '2026-12-02', return: '2026-12-12', total: 640000, taxRate: 5, status: 'Confirmed', stage: 5, invoice: 'INV-1082' },
    { id: 'HT-26077', customer: 'Anita Joshi', phone: '9967233021', trip: 'Kerala', type: 'Domestic', services: 'Hotel, Transfer', departure: '2026-09-20', return: '2026-09-25', total: 98000, taxRate: 5, status: 'Pending', stage: 2, invoice: '' }
  ],
  visas: [
    { id: 'V-301', applicant: 'Sameer More', country: 'Schengen', appointment: '2026-09-05', done: 8, total: 10, fee: 9800, status: 'Documents pending' },
    { id: 'V-302', applicant: 'Ayesha Khan', country: 'Dubai', appointment: '', done: 6, total: 6, fee: 7200, status: 'Approved' },
    { id: 'V-303', applicant: 'Rahul Shah', country: 'Singapore', appointment: '2026-08-18', done: 7, total: 7, fee: 3400, status: 'Submitted' }
  ],
  payments: [
    { id: 'PAY-1082', invoice: 'INV-1082', customer: 'Sameer More', total: 640000, received: 320000, due: '2026-08-30', receipts: [{ id: 'REC-OPEN-1082', amount: 320000, date: '2026-08-20', method: 'Bank transfer', reference: 'Opening balance' }] },
    { id: 'PAY-1078', invoice: 'INV-1078', customer: 'Anita Joshi', total: 98000, received: 98000, due: '2026-08-05', receipts: [{ id: 'REC-OPEN-1078', amount: 98000, date: '2026-08-05', method: 'UPI', reference: 'Opening balance' }] }
  ],
  suppliers: [
    { id: 'SUP-SNOWLINE', name: 'Snowline Hotels', type: 'Hotel', location: 'Kashmir', outstanding: 48000, rating: 4.8, status: 'Available', lastRate: 4250 },
    { id: 'SUP-DESERT', name: 'Desert Pearl DMC', type: 'DMC', location: 'Dubai', outstanding: 110000, rating: 4.6, status: 'Available', lastRate: 12900 },
    { id: 'SUP-KONKAN', name: 'Konkan Wheels', type: 'Transport', location: 'Maharashtra', outstanding: 12500, rating: 4.7, status: 'Limited', lastRate: 4800 }
  ],
  packages: [
    { id: 'PKG-JAPAN', name: 'Japan, beyond the postcard', destination: 'Japan', duration: '7N / 8D', nights: 7, route: 'Tokyo · Hakone · Kyoto · Osaka', price: 159900, type: 'International', theme: 'Culture', flights: 'Included', stars: 4, color: 'japan', tagline: 'Mount Fuji, a tea ceremony & Kyoto lanes', itinerary: 'Arrive in Tokyo|Airport transfer and time to settle in.\nTokyo stories|Neighbourhoods, markets and a skyline viewpoint.\nTowards Hakone|Scenic transfer and time around Mount Fuji.\nKyoto bound|Rail journey and an evening at leisure.\nKyoto traditions|Temples, old lanes and a tea experience.\nNara day|A flexible excursion to Nara.\nOsaka energy|Street food districts and free time.\nHomeward bound|Breakfast and airport transfer.', details: '7 nights, breakfast, transfers, listed sightseeing and an illustrative economy flight allowance. Final suppliers, visa, insurance and taxes require confirmation.' },
    { id: 'PKG-KASHMIR', name: 'Kashmir Paradise', destination: 'Kashmir', duration: '6N / 7D', nights: 6, route: 'Srinagar · Gulmarg · Pahalgam', price: 42500, type: 'Domestic', theme: 'Family', flights: 'Not included', stars: 3, color: 'kashmir', tagline: 'Valley drives, a shikara & mountain air', itinerary: 'Arrive in Srinagar|Pickup and hotel check-in.\nSrinagar|Gardens and a relaxed shikara ride.\nGulmarg|Full-day mountain excursion.\nPahalgam|Scenic drive and local time.\nValley day|Flexible sightseeing with your driver.\nSrinagar at leisure|Shopping and optional activities.\nDeparture|Breakfast and airport transfer.', details: '6 nights, breakfast, ground transfers and listed sightseeing. Flights, optional activities and personal expenses are excluded.' },
    { id: 'PKG-BALI', name: 'Bali, a little closer to paradise', destination: 'Bali', duration: '5N / 6D', nights: 5, route: 'Ubud · Nusa Penida · Seminyak', price: 58900, type: 'International', theme: 'Honeymoon', flights: 'Not included', stars: 4, color: 'bali', tagline: 'Rice terraces, island views & slow mornings', itinerary: 'Welcome to Bali|Airport transfer and check-in.\nUbud|Rice terraces and local culture.\nNusa Penida|Island excursion subject to sea conditions.\nSeminyak|Transfer to the coast and leisure time.\nA day your way|Optional activities priced separately.\nDeparture|Breakfast and airport transfer.', details: '5 nights, breakfast, airport transfers and listed sightseeing. Flights, visa, insurance and optional activities are excluded.' },
    { id: 'PKG-DUBAI', name: 'Dubai Delight', destination: 'Dubai', duration: '4N / 5D', nights: 4, route: 'Dubai · Abu Dhabi · Arabian Desert', price: 58900, type: 'International', theme: 'Family', flights: 'Included', stars: 4, color: 'dubai', tagline: 'A skyline cruise & an evening in the dunes', itinerary: 'Arrive in Dubai|Airport pickup and hotel check-in.\nDubai city|Old Dubai, skyline stops and evening cruise.\nDesert day|Free morning and desert excursion.\nAbu Dhabi|Guided city excursion.\nDeparture|Breakfast and airport transfer.', details: '4 nights, breakfast, transfers, city tour, desert excursion and illustrative return economy flight allowance. Final availability and taxes require confirmation.' },
    { id: 'PKG-KERALA', name: 'Kerala Backwaters', destination: 'Kerala', duration: '5N / 6D', nights: 5, route: 'Munnar · Thekkady · Alleppey', price: 36800, type: 'Domestic', theme: 'Family', flights: 'Not included', stars: 3, color: 'kerala', tagline: 'Tea gardens, spice trails & a houseboat', itinerary: 'Kochi to Munnar|Pickup and scenic hill drive.\nMunnar|Tea gardens and viewpoints.\nThekkady|Drive and spice-garden visit.\nThekkady at leisure|Optional wildlife activities.\nAlleppey|Houseboat check-in and backwaters.\nDeparture|Breakfast and Kochi transfer.', details: '4 hotel nights, 1 houseboat night, breakfast, ground transfers and sightseeing. Flights and optional activities are excluded.' },
    { id: 'PKG-JAPAN-ALPINE', name: 'The Japan alpine journey', destination: 'Japan', duration: '9N / 10D', nights: 9, route: 'Tokyo · Matsumoto · Takayama · Kyoto', price: 194500, type: 'International', theme: 'Adventure', flights: 'Included', stars: 4, color: 'japan', tagline: 'Mountain towns & heritage railway journeys', itinerary: 'Tokyo arrival|Transfer and check-in.\nTokyo|City exploration.\nMatsumoto|Rail to the mountains.\nAlpine route|Scenic transport day.\nTakayama|Old town and markets.\nShirakawa-go|Heritage village excursion.\nKyoto|Travel and leisure.\nKyoto|Temples and neighbourhoods.\nFree day|Flexible final day.\nDeparture|Airport transfer.', details: 'Illustrative 9-night itinerary with 4-star stays, breakfast, transfers and a return economy flight allowance.' },
    { id: 'PKG-DUBAI-WEEKEND', name: 'Dubai in a long weekend', destination: 'Dubai', duration: '3N / 4D', nights: 3, route: 'Old Dubai · Downtown · Dubai Marina', price: 37900, type: 'International', theme: 'Culture', flights: 'Not included', stars: 3, color: 'dubai', tagline: 'Old-world souks & a modern skyline', itinerary: 'Arrival|Transfer and marina evening.\nOld and new Dubai|Guided city day.\nDesert or leisure|Choose an optional excursion.\nDeparture|Airport transfer.', details: '3 nights, breakfast and transfers. Flights and optional activities are excluded.' },
    { id: 'PKG-KERALA-COUPLE', name: 'Kerala, just the two of you', destination: 'Kerala', duration: '4N / 5D', nights: 4, route: 'Munnar · Kumarakom · Kochi', price: 44900, type: 'Domestic', theme: 'Honeymoon', flights: 'Not included', stars: 4, color: 'kerala', tagline: 'Lakeside stays & unhurried afternoons', itinerary: 'Kochi to Munnar|Pickup and transfer.\nMunnar|Tea country at leisure.\nKumarakom|Drive to the lake.\nKumarakom|Relaxed lakeside day.\nDeparture|Kochi transfer.', details: '4 nights, breakfast, private transfers and listed sightseeing. Flights are excluded.' }
  ]
};

const validators = {
  leads: x => ({ ...x, name: text(x.name, 'Name'), phone: phone(x.phone), destination: text(x.destination, 'Destination'), date: date(x.date, 'Travel date'), source: one(x.source, ['Website', 'WhatsApp', 'Instagram', 'Referral'], 'Source'), owner: text(x.owner, 'Owner'), followup: optionalDate(x.followup, 'Follow-up date'), status: one(x.status, leadStates, 'Status') }),
  customers: x => ({ ...x, name: text(x.name, 'Name'), phone: phone(x.phone), documents: String(x.documents || '').trim(), lastTrip: String(x.lastTrip || '').trim() }),
  bookings: x => { const departure = date(x.departure, 'Departure'), returnDate = date(x.return, 'Return'); if (returnDate < departure) fail('Return cannot be before departure.'); return { ...x, customer: text(x.customer, 'Customer'), phone: phone(x.phone), trip: text(x.trip, 'Destination'), type: one(x.type, ['Domestic', 'International'], 'Trip type'), services: text(x.services, 'Services'), departure, return: returnDate, total: money(x.total, 'Total'), taxRate: number(x.taxRate ?? 5, 0, 100, 'Tax rate'), status: one(x.status || 'Pending', bookingStates, 'Status'), stage: number(x.stage ?? 1, 1, 5, 'Stage', true), invoice: String(x.invoice || '') }; },
  visas: x => { const total = number(x.total, 1, 100, 'Checklist total', true), done = number(x.done, 0, total, 'Documents complete', true), status = one(x.status, visaStates, 'Status'); if (['Submitted', 'Approved'].includes(status) && done !== total) fail('Complete the checklist before marking Submitted or Approved.'); return { ...x, applicant: text(x.applicant, 'Applicant'), country: text(x.country, 'Country'), appointment: optionalDate(x.appointment, 'Appointment'), fee: money(x.fee, 'Visa fee'), total, done, status }; },
  payments: x => { const total = money(x.total, 'Invoice total'), received = money(x.received ?? 0, 'Received'); if (received > total) fail('Received amount cannot exceed the invoice total.'); return { ...x, invoice: text(x.invoice, 'Invoice').toUpperCase(), customer: text(x.customer, 'Customer'), total, received, due: date(x.due, 'Due date'), receipts: Array.isArray(x.receipts) ? x.receipts : [] }; },
  suppliers: x => ({ ...x, name: text(x.name, 'Supplier name'), type: one(x.type, ['Hotel', 'DMC', 'Transport', 'Airline', 'Other'], 'Supplier type'), location: text(x.location, 'Location'), lastRate: money(x.lastRate, 'Saved rate'), outstanding: money(x.outstanding, 'Outstanding'), rating: number(x.rating, 0, 5, 'Rating'), status: one(x.status, ['Available', 'Limited', 'Unavailable'], 'Availability') }),
  packages: x => ({ ...x, name: text(x.name, 'Package name'), destination: text(x.destination, 'Destination'), duration: text(x.duration, 'Duration'), nights: number(x.nights, 1, 60, 'Nights', true), route: text(x.route, 'Route'), price: money(x.price, 'Price'), type: one(x.type, ['Domestic', 'International'], 'Trip type'), theme: one(x.theme, ['Family', 'Honeymoon', 'Adventure', 'Culture'], 'Theme'), flights: one(x.flights, ['Included', 'Not included'], 'Flight inclusion'), stars: number(x.stars, 1, 5, 'Hotel stars'), color: one(x.color || 'kerala', ['japan', 'kashmir', 'bali', 'dubai', 'kerala'], 'Card colour'), tagline: text(x.tagline, 'Tagline'), itinerary: text(x.itinerary, 'Itinerary'), details: text(x.details, 'Details') })
};

function fail(message, status = 400) { const error = new Error(message); error.status = status; throw error; }
function text(value, label) { const out = String(value ?? '').trim(); if (!out) fail(`${label} is required.`); if (out.length > 2000) fail(`${label} is too long.`); return out; }
function one(value, values, label) { if (!values.includes(value)) fail(`Choose a valid ${label.toLowerCase()}.`); return value; }
function number(value, min, max, label, integer = false) { const out = Number(value); if (!Number.isFinite(out) || out < min || out > max || integer && !Number.isInteger(out)) fail(`${label} must be ${integer ? 'a whole number' : 'a number'} from ${min} to ${max}.`); return out; }
function money(value, label) { const out = typeof value === 'string' ? Number(value.replace(/[₹,\s]/g, '').replace(/^Rs\.?/i, '').replace(/^INR/i, '')) : Number(value); if (!Number.isFinite(out) || out < 0 || out > 1e10) fail(`${label} must be a valid positive rupee amount.`); return Math.round(out * 100) / 100; }
function date(value, label) { const out = String(value || ''); const d = new Date(`${out}T12:00:00Z`); if (!/^\d{4}-\d{2}-\d{2}$/.test(out) || Number.isNaN(d.valueOf()) || d.toISOString().slice(0, 10) !== out) fail(`${label} must be a real date.`); return out; }
function optionalDate(value, label) { return value ? date(value, label) : ''; }
function phone(value) { const out = String(value || '').replace(/[\s()-]/g, ''); if (!/^\+?\d{10,15}$/.test(out)) fail('Enter a valid 10–15 digit phone number.'); return out; }
function idFor(collection) { return `${collection.slice(0, 3).toUpperCase()}-${randomUUID().replaceAll('-', '').slice(0, 12).toUpperCase()}`; }

export function openDatabase(filename) {
  const sql = new DatabaseSync(filename);
  sql.exec(`PRAGMA journal_mode = WAL; PRAGMA busy_timeout = 5000;
    CREATE TABLE IF NOT EXISTS records (collection TEXT NOT NULL,id TEXT NOT NULL,data TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY (collection,id));
    CREATE TABLE IF NOT EXISTS meta (key TEXT PRIMARY KEY,data TEXT NOT NULL);`);
  if (!sql.prepare('SELECT COUNT(*) count FROM records').get().count) {
    const insert = sql.prepare('INSERT INTO records (collection,id,data) VALUES (?,?,?)'); sql.exec('BEGIN');
    try { for (const [collection, rows] of Object.entries(seed)) for (const row of rows) insert.run(collection, row.id, JSON.stringify(validators[collection](row))); sql.exec('COMMIT'); }
    catch (error) { sql.exec('ROLLBACK'); throw error; }
  }
  const defaults = {
    settings: { business: 'Hindavi Tourism', gstin: '27ABCDE1234F1Z5', phone: '919820000000', email: 'hello@hindavitourism.in', address: 'Pune, Maharashtra', payment: 'UPI / Bank transfer', terms: 'Prices and availability are subject to supplier confirmation.' },
    quote: { customer: '', phone: '', email: '', destination: '', start: '', duration: '', travellers: 2, departure: 'Pune', itinerary: '', details: '', taxRate: 5, items: [] }, explorer: { saved: [], recent: [] }
  };
  const putMeta = sql.prepare('INSERT OR IGNORE INTO meta (key,data) VALUES (?,?)'); for (const [key, value] of Object.entries(defaults)) putMeta.run(key, JSON.stringify(value));
  const api = {
    close: () => sql.close(),
    list(collection) { ensureCollection(collection); return sql.prepare('SELECT data FROM records WHERE collection=? ORDER BY created_at DESC').all(collection).map(row => JSON.parse(row.data)); },
    get(collection, id) { ensureCollection(collection); const row = sql.prepare('SELECT data FROM records WHERE collection=? AND id=?').get(collection, id); if (!row) fail('Record not found.', 404); return JSON.parse(row.data); },
    save(collection, value, id = '') { ensureCollection(collection); const record = validators[collection]({ ...value, id: id || value.id || idFor(collection) }); if (id && !sql.prepare('SELECT 1 FROM records WHERE collection=? AND id=?').get(collection, id)) fail('Record not found.', 404); sql.prepare('INSERT INTO records (collection,id,data) VALUES (?,?,?) ON CONFLICT(collection,id) DO UPDATE SET data=excluded.data,updated_at=CURRENT_TIMESTAMP').run(collection, record.id, JSON.stringify(record)); return record; },
    remove(collection, id) { ensureCollection(collection); const result = sql.prepare('DELETE FROM records WHERE collection=? AND id=?').run(collection, id); if (!result.changes) fail('Record not found.', 404); },
    state() { return Object.fromEntries([...collections.map(name => [name, api.list(name)]), ...['settings', 'quote', 'explorer'].map(key => [key, api.meta(key)])]); },
    meta(key, value) { if (!['settings', 'quote', 'explorer'].includes(key)) fail('Unknown setting.', 404); if (value === undefined) return JSON.parse(sql.prepare('SELECT data FROM meta WHERE key=?').get(key).data); if (!value || typeof value !== 'object' || Array.isArray(value)) fail('Setting must be an object.'); sql.prepare('UPDATE meta SET data=? WHERE key=?').run(JSON.stringify(value), key); return value; },
    receipt(paymentId, input) { const payment = api.get('payments', paymentId), amount = money(input.amount, 'Payment amount'); if (amount <= 0) fail('Payment amount must be greater than zero.'); if (Math.round((payment.received + amount) * 100) > Math.round(payment.total * 100)) fail('Payment exceeds the outstanding balance.'); const receipt = { id: `REC-${randomUUID().slice(0, 8).toUpperCase()}`, amount, date: date(input.date, 'Payment date'), method: one(input.method, ['UPI', 'Bank transfer', 'Cash', 'Card', 'Other'], 'Payment method'), reference: String(input.reference || '').trim() }; payment.receipts = [...payment.receipts, receipt]; payment.received = Math.round((payment.received + amount) * 100) / 100; api.save('payments', payment, payment.id); return { payment, receipt }; },
    confirmBooking(id) { sql.exec('BEGIN IMMEDIATE'); try { const booking = api.get('bookings', id); if (booking.status === 'Confirmed' && booking.invoice) { sql.exec('COMMIT'); return { booking, payment: api.list('payments').find(p => p.invoice === booking.invoice) }; } const invoice = booking.invoice || `INV-${String(Date.now()).slice(-6)}`; booking.status = 'Confirmed'; booking.stage = 5; booking.invoice = invoice; api.save('bookings', booking, id); let payment = api.list('payments').find(p => p.invoice === invoice); if (!payment) payment = api.save('payments', { invoice, customer: booking.customer, total: booking.total, received: 0, due: booking.departure, receipts: [] }); sql.exec('COMMIT'); return { booking, payment }; } catch (error) { sql.exec('ROLLBACK'); throw error; } },
    backup() { return { schema: 1, exportedAt: new Date().toISOString(), ...api.state() }; },
    restore(value) { if (!value || value.schema !== 1) fail('Unsupported backup file.'); for (const collection of collections) if (!Array.isArray(value[collection])) fail(`Backup is missing ${collection}.`); sql.exec('BEGIN IMMEDIATE'); try { sql.exec('DELETE FROM records'); const insert = sql.prepare('INSERT INTO records (collection,id,data) VALUES (?,?,?)'); for (const collection of collections) for (const row of value[collection]) { const record = validators[collection](row); if (!record.id) fail(`A ${collection} record has no id.`); insert.run(collection, record.id, JSON.stringify(record)); } for (const key of ['settings', 'quote', 'explorer']) api.meta(key, value[key] || {}); sql.exec('COMMIT'); return api.state(); } catch (error) { sql.exec('ROLLBACK'); throw error; } }
  }; return api;
}

function ensureCollection(collection) { if (!collections.includes(collection)) fail('Unknown collection.', 404); }
const flightRoutes = new Map([['delhi|pune',6500],['bengaluru|pune',4800],['goa|pune',4200],['delhi|mumbai',6200],['dubai|mumbai',15500],['mumbai|tokyo',48500],['bali|mumbai',27000],['delhi|srinagar',7400],['delhi|tokyo',46500],['delhi|dubai',18000],['bengaluru|delhi',7100],['bengaluru|kochi',3600],['bengaluru|singapore',16800],['kochi|mumbai',5900]]);
const cabRoutes = new Map([['mumbai|pune',150],['lonavala|pune',65],['lonavala|mumbai',85],['agra|delhi',230],['kochi|munnar',130],['gulmarg|srinagar',55]]);
const normal = value => String(value || '').trim().toLowerCase().replace(/^new delhi$/, 'delhi').replace(/^bangalore$/, 'bengaluru').replace(/^cochin$/, 'kochi');
const routeKey = (a,b) => [normal(a),normal(b)].sort().join('|');
const days = (a,b) => Math.round((Date.parse(`${b}T12:00:00Z`) - Date.parse(`${a}T12:00:00Z`)) / 86400000);

export function estimateTravel(input) {
  const mode = one(input.mode, ['flights','hotels','cabs','trains','bus'], 'Travel mode'), from = text(input.from,'Origin'), to = text(input.to,'Destination'); if (normal(from) === normal(to)) fail('Origin and destination must be different.');
  const departure = date(input.departure,'Departure'), adults = number(input.adults ?? 1,1,20,'Adults',true), children = number(input.children ?? 0,0,10,'Children',true), infants = number(input.infants ?? 0,0,10,'Infants',true), rooms = number(input.rooms ?? 1,1,20,'Rooms',true); if (infants > adults) fail('Each infant needs an accompanying adult.'); const base = { mode,from,to,departure,adults,children,infants,rooms };
  if (['trains','bus'].includes(mode)) return { ...base,manual:true,message:'Enter the total returned by your supplier; no live inventory is connected.' };
  if (mode === 'flights') {
    const trip = one(input.trip || 'oneway',['oneway','return','multi'],'Trip type'), cabin = one(input.cabin || 'Economy',['Economy','Premium economy','Business'],'Cabin'); const legs = trip === 'multi' ? structuredClone(input.legs) : [{ from,to,date:departure }]; if (!Array.isArray(legs) || legs.length < 1 || legs.length > 4) fail('Choose one to four flight legs.');
    if (trip === 'return') { const returnDate = date(input.returnDate,'Return date'); if (returnDate < departure) fail('Return cannot be before departure.'); legs.push({ from:to,to:from,date:returnDate }); }
    let previous = departure, routeTotal = 0; for (const leg of legs) { leg.from=text(leg.from,'Leg origin'); leg.to=text(leg.to,'Leg destination'); leg.date=date(leg.date,'Leg date'); if(normal(leg.from)===normal(leg.to))fail('Each leg needs different cities.'); if(leg.date<previous)fail('Multi-city dates must be in order.'); previous=leg.date; const fare=flightRoutes.get(routeKey(leg.from,leg.to)); if(!fare)return {...base,manual:true,message:'This route is outside the offline example table. Enter a supplier estimate instead.'}; routeTotal+=fare; }
    const cabinRate={Economy:1,'Premium economy':1.5,Business:2.5}[cabin],price=Math.round(routeTotal*cabinRate*(adults+children*.75+infants*.1)*100)/100; return {...base,offers:[{id:'flight',name:`${cabin} · ${trip==='oneway'?'one way':trip==='return'?'round trip':'multi-city'}`,route:legs.map(x=>`${x.from} → ${x.to}`).join(' / '),price,basis:'Planning allowance only; airline, schedule, baggage and terms require confirmation.'}]};
  }
  if (mode === 'hotels') { const checkout=date(input.returnDate,'Check-out'),nights=days(departure,checkout); if(nights<1)fail('Check-out must be after check-in.'); if(rooms>adults||adults+children>rooms*3)fail('Choose at least one adult per room and no more than three guests per room.'); return {...base,nights,offers:[['Comfort stay',2800],['Premium stay',5200],['Luxury stay',9500]].map(([name,rate],index)=>({id:`hotel-${index}`,name,route:to,price:rate*rooms*nights,basis:`₹${rate.toLocaleString('en-IN')} × ${rooms} room(s) × ${nights} night(s); named hotel and inclusions require confirmation.`}))}; }
  const km=cabRoutes.get(routeKey(from,to)); if(!km)return {...base,manual:true,message:'This route has no offline distance estimate. Enter the supplier total instead.'}; return {...base,offers:[['Sedan',14,3],['SUV',19,6],['Traveller van',27,12]].map(([name,rate,capacity],index)=>{const cars=Math.ceil((adults+children)/capacity);return {id:`cab-${index}`,name:`${cars} × ${name}`,route:`${from} → ${to}`,price:(km*rate+500)*cars,basis:`${km} example km × ₹${rate}/km + ₹500 per vehicle; tolls and final distance require confirmation.`};})};
}
