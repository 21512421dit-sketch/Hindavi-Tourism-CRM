import test from 'node:test';
import assert from 'node:assert/strict';
import { mkdtempSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { createServer } from './server.js';
let server,base;
test.before(async()=>{server=createServer({database:join(mkdtempSync(join(tmpdir(),'hindavi-test-')),'test.sqlite')});await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));base=`http://127.0.0.1:${server.address().port}`;});
test.after(()=>server ? new Promise(resolve=>server.close(resolve)) : undefined);
const request=async(path,options={})=>{const response=await fetch(base+path,{...options,headers:options.body?{'Content-Type':'application/json',...options.headers}:options.headers});const value=response.status===204?null:await response.json();return{response,value};};
test('health, static frontend and seeded state are available',async()=>{assert.deepEqual((await request('/api/health')).value,{ok:true});const state=(await request('/api/state')).value;assert.equal(state.leads.length,3);assert.ok(state.packages.length>=8);assert.equal(state.settings.business,'Hindavi Tourism');const html=await fetch(base).then(r=>r.text());assert.match(html,/Hindavi Tourism CRM/);});
test('CRUD validates input and persists records',async()=>{const invalid=await request('/api/leads',{method:'POST',body:JSON.stringify({name:''})});assert.equal(invalid.response.status,400);const created=await request('/api/leads',{method:'POST',body:JSON.stringify({name:'Test Lead',phone:'9876543210',destination:'Goa',date:'2026-12-01',source:'Referral',owner:'Snehal',followup:'',status:'New'})});assert.equal(created.response.status,201);assert.match(created.value.id,/^LEA-/);created.value.status='Follow-up';const updated=await request(`/api/leads/${created.value.id}`,{method:'PUT',body:JSON.stringify(created.value)});assert.equal(updated.value.status,'Follow-up');assert.equal((await request(`/api/leads/${created.value.id}`)).value.name,'Test Lead');});
test('travel estimates cover flights, hotels, cabs and manual fallbacks',async()=>{const common={from:'Pune',to:'Delhi',departure:'2026-12-01',adults:2,children:1,infants:1,rooms:1};const flight=(await request('/api/travel/estimate',{method:'POST',body:JSON.stringify({...common,mode:'flights',trip:'oneway',cabin:'Economy'})})).value;assert.equal(flight.offers[0].price,18525);const hotel=(await request('/api/travel/estimate',{method:'POST',body:JSON.stringify({...common,mode:'hotels',to:'Kerala',returnDate:'2026-12-06'})})).value;assert.equal(hotel.offers[0].price,14000);const cab=(await request('/api/travel/estimate',{method:'POST',body:JSON.stringify({...common,mode:'cabs',to:'Mumbai'})})).value;assert.equal(cab.offers[0].price,2600);const unknown=(await request('/api/travel/estimate',{method:'POST',body:JSON.stringify({...common,mode:'flights',to:'Unknown',trip:'oneway',cabin:'Economy'})})).value;assert.equal(unknown.manual,true);});
test('payment receipts cannot overpay an invoice',async()=>{const state=(await request('/api/state')).value,payment=state.payments.find(x=>x.invoice==='INV-1082');const added=await request(`/api/payments/${payment.id}/receipts`,{method:'POST',body:JSON.stringify({amount:1000.50,date:'2026-08-28',method:'UPI',reference:'TEST'})});assert.equal(added.response.status,201);assert.equal(added.value.payment.received,321000.5);const rejected=await request(`/api/payments/${payment.id}/receipts`,{method:'POST',body:JSON.stringify({amount:9999999,date:'2026-08-28',method:'Cash'})});assert.equal(rejected.response.status,400);assert.match(rejected.value.error,/exceeds/);});
test('booking confirmation creates one invoice and is idempotent',async()=>{const first=(await request('/api/bookings/HT-26077/confirm',{method:'POST'})).value,second=(await request('/api/bookings/HT-26077/confirm',{method:'POST'})).value;assert.equal(first.booking.status,'Confirmed');assert.equal(second.booking.invoice,first.booking.invoice);const state=(await request('/api/state')).value;assert.equal(state.payments.filter(x=>x.invoice===first.booking.invoice).length,1);});
test('backup restore rejects malformed data without replacing state',async()=>{const before=(await request('/api/state')).value.leads.length,bad=await request('/api/restore',{method:'POST',body:JSON.stringify({schema:1,leads:[]})});assert.equal(bad.response.status,400);assert.equal((await request('/api/state')).value.leads.length,before);});
test('booking dates and opening invoice balances are protected',async()=>{const badBooking=await request('/api/bookings',{method:'POST',body:JSON.stringify({customer:'Test',phone:'9876543210',trip:'Goa',type:'Domestic',services:'Hotel',departure:'2026-12-10',return:'2026-12-01',total:10000,taxRate:5,status:'Pending',stage:1})});assert.equal(badBooking.response.status,400);assert.match(badBooking.value.error,/Return/);const badInvoice=await request('/api/payments',{method:'POST',body:JSON.stringify({invoice:'INV-BAD',customer:'Test',total:1000,received:2000,due:'2026-12-01'})});assert.equal(badInvoice.response.status,400);assert.match(badInvoice.value.error,/exceed/);});
